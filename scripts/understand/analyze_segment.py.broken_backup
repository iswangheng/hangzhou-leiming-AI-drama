"""
逐段分析模块
使用Gemini API分析每个片段，识别高光点/钩子点
"""
import json
import re
import time
import base64
import requests
from typing import List, Optional
from dataclasses import dataclass
from concurrent.futures import ThreadPoolExecutor, as_completed

try:
    from scripts.config import TrainingConfig
    from scripts.dataclasses import KeyFrame, ASRSegment
    from scripts.understand.extract_segments import VideoSegment
except ImportError:
    @dataclass
    class KeyFrame:
        frame_path: str
        timestamp_ms: int
        
    @dataclass
    class ASRSegment:
        text: str
        start: float
        end: float
    
    @dataclass
    class VideoSegment:
        episode: int
        start_time: int
        end_time: int
        keyframes: List[KeyFrame]
        asr_segments: List[ASRSegment]


# 分析Prompt模板 - V10版本（Few-Shot Prompting + 示例驱动学习）
#
# V10核心创新：
# 1. Few-Shot Prompting - 给出具体ASR文本示例，让AI对比学习
# 2. 三步判断法 - 让AI回答"观众想知道什么"而不是检查条件
# 3. 悬念分析字段 - 要求AI提供可解释的推理过程
# 4. 不过滤"对话突然中断"类型 - 而是提高判断准确性
#
# 预期效果：
# - 召回率: 83.3% → 70-75% (略降，但可接受)
# - 精确率: 26.3% → 35-40% (显著提升)
# - "对话突然中断"准确率: 43% → 75%+ (大幅提升)
ANALYZE_PROMPT = """你是一位资深的短剧剪辑分析师。我给你展示4个真实示例，让你学会区分真钩子和假钩子。

## 📚 示例对比学习

仔细阅读下面的4个示例，理解为什么有些"对话突然中断"是真钩子，有些是假钩子。

### ✅ 示例A: 真钩子的"对话突然中断"

**ASR文本**: "夫人正要说出陆大人的秘密，突然被闯入的家丁打断"

**悬念分析**:
- 观众想知道什么？→ **陆大人的秘密是什么？**（明确问题）
- 这个信息有多关键？→ 非常关键，"秘密"会改变剧情走向
- 中断有多意外？→ 第三方突然闯入，非常意外

**悬念强度**: 9/10

**结论**: 这是真钩子，应该标记

---

### ❌ 示例B: 假钩子的"对话突然中断"

**ASR文本**: "男主说：那就请太夫人。对话结束"

**悬念分析**:
- 观众想知道什么？→ 不迫切想知道（只是普通请求）
- 这个信息有多关键？→ 不关键，日常对话
- 中断有多意外？→ 对话自然说完，不是被打断

**悬念强度**: 2/10

**结论**: 这不是钩子，不要标记

---

### ✅ 示例C: 真钩子的"对话突然中断"

**ASR文本**: "女主说：我要进去看陆停松到底怎么了。话音刚落，画面结束"

**悬念分析**:
- 观众想知道什么？→ **她会看到什么？陆停松怎么了？**（明确问题）
- 这个信息有多关键？→ 关键，角色即将发现真相
- 中断有多意外？→ 行动前一刻画面停住，制造悬念

**悬念强度**: 8/10

**结论**: 这是真钩子，应该标记

---

### ❌ 示例D: 假钩子的"对话突然中断"

**ASR文本**: "女主说：我这么好语... 镜头切换到下一场景"

**悬念分析**:
- 观众想知道什么？→ 没有明确悬念
- 这个信息有多关键？→ 不关键，普通对话
- 中断有多意外？→ 镜头切换，不是意外中断

**悬念强度**: 1/10

**结论**: 这不是钩子，不要标记

---

## 🎯 核心判断方法

对于"对话突然中断"类型，请使用**三步判断法**：

### 步骤1: 回答问题
如果当前片段可能是"对话突然中断"，请回答：
1. **观众想知道什么？**（必须给出明确、具体的问题）
2. **这个信息有多关键？**（秘密/真相/决定 vs 普通对话）
3. **中断有多意外？**（第三方闯入/突发事件 vs 自然结束/场景切换）

### 步骤2: 评分悬念强度（0-10分）
- **8-10分**: 强烈想知道，必须标记
- **6-7分**: 有点想知道，可以标记
- **<6分**: 不想知道，不要标记

### 步骤3: 做出决策
- ✅ 如果悬念强度≥6分，且能明确回答"观众想知道什么" → **标记**
- ❌ 如果悬念强度<6分，或无法回答"观众想知道什么" → **不标记**

---

## 📺 分析当前片段

**时间范围**: {START}-{END}秒（第{EPISODE}集）
**ASR文本**: {ASR_TEXT}

**可选的类型**:
{HOOK_TYPES}

---

## 🔍 分析步骤

1. **理解情节**: 阅读ASR文本，理解剧情走向
2. **判断类型**: 检查是否匹配上述某个钩子类型
3. **三步判断**: 如果是"对话突然中断"，使用三步判断法
4. **精确定位**: 如果是钩子点，返回精确的秒数
5. **置信度评分**: 根据悬念强度，给出0-10分

---

## ⚙️ 特别规则

1. **第1集开头**: 如果是第1集的0-30秒范围，自动识别为"开篇高光点"
2. **类型优先**: 优先使用上述类型，实在匹配不上可以标记为"未知"
3. **置信度**: 悬念强度≥6分的钩子点，置信度≥7.0
4. **不要漏掉**: 明显的情节转折、秘密揭露、情感爆发 → 应该标记

---

## 📤 输出格式

```json
{{
  "highlight": {{
    "exists": true/false,
    "preciseSecond": 125,
    "type": "从上述类型中选择",
    "confidence": 8.5,
    "reasoning": "简短说明"
  }},
  "hook": {{
    "exists": true/false,
    "preciseSecond": 366,
    "type": "从上述类型中选择（如：对话突然中断）",
    "confidence": 9.2,
    "reasoning": "简短说明",
    "suspense_analysis": {{
      "what_question": "观众想知道什么？（必须明确）",
      "why_care": "为什么重要？",
      "how_unexpected": "有多意外？",
      "suspense_score": 9
    }}
  }}
}}
```

**关键**: 如果`hook.exists=true`且类型是"对话突然中断"，必须填写`suspense_analysis`字段。如果无法填写`what_question`，则`hook.exists=false`。

**只返回JSON，不要其他文字。**
"""

## ⭐ 高光点定义

- **含义**: 从**这个时刻开始**，观众更愿意看下去
- **特征**: 吸引力最强的开始时刻（冲突爆发、悬念引入、情感高潮）
- **关键**: 返回窗口内**精确的秒数**，不是窗口开始时间！
- **示例**: 窗口350-410秒，高光点在366秒 → 返回 366

---

## 🪝 钩子点定义

- **含义**: 播放到**这个时刻突然结束**，观众迫切想知道后续
- **特征**: 悬念最强的结束时刻（欲知后事如何、关键信息被截断、反转前一刻）
- **关键**: 返回窗口内**精确的秒数**，不是窗口开始时间！
- **示例**: 窗口350-410秒，钩子点在395秒 → 返回 395

---

## 📋 常见钩子点位置（按强度排序）

### 🔥 强度最高（优先考虑）
1. **情节重大转折**（反转、突变、意外、命运改变）
2. **秘密揭露或真相揭示**（关键信息暴露、身份揭晓）
3. **悬念反转时刻**（剧情急转直下、意想不到的变化）
4. **冲突爆发前一刻**（争吵、对抗、矛盾即将激化）
5. **情感爆发顶点**（愤怒、震惊、崩溃的极致时刻）
6. **信息截断/欲知后事**（关键时刻被打断、画面停在悬念处）

### ⚠️ 谨慎标记的类型（容易误判）
7. **对话突然中断**（必须满足严格的3个条件，见下文）

---

## ❌ 以下情况不是钩子点（不要标记）

### 对话中断类的假阳性
1. **普通对话结束**
   - ❌ 场景A说完话，场景B开始说话
   - ❌ 角色说完一句完整的话，镜头切换
   - ✅ 必须是: 在关键信息即将说出时被打断

2. **场景切换导致的停顿**
   - ❌ 从一个场景切换到另一个场景
   - ❌ 镜头转换、时间地点变化
   - ✅ 必须是: 同一场景内，情节被意外打断

3. **情感自然的结束**
   - ❌ 角色表达完情感后自然平静下来
   - ❌ 对话达成共识或结束
   - ✅ 必须是: 情绪达到高点，但后续未知

### 其他假阳性
4. **普通情节推进**
   - ❌ 日常对话、常规剧情发展
   - ❌ 铺垫性的情节（为后续做准备的）
   - ✅ 必须是: 改变剧情方向的转折点

5. **已知信息的重复**
   - ❌ 观众已经知道的信息
   - ❌ 前面已经揭示过的秘密
   - ✅ 必须是: 新的、重要的、观众不知道的信息

---

## ⚠️ "对话突然中断"的严格判断标准

**只有同时满足以下3个条件，才能标记为"对话突然中断"**:

### 条件1: 信息必须关键（重要性）
✅ 正确示例:
- 角色即将说出秘密/真相时被打断
- 身份即将揭晓时被切断
- 关键决定即将做出时被打断
- 危险即将被揭示时被打断

❌ 错误示例:
- 普通对话被打断（日常闲聊、问候）
- 重复已知信息
- 不重要的细节被打断

### 条件2: 中断必须意外（突发性）
✅ 正确示例:
- 第三方突然闯入打断对话
- 突发事件（爆炸、闯入、晕倒）
- 画面突然黑屏/转场
- 被暴力打断（推倒、捂嘴、绑架）

❌ 错误示例:
- 对话自然结束（说完了）
- 场景切换（时间地点变化）
- 镜头转换（不是中断，是转场）

### 条件3: 观众必须迫切想知道后续（悬念强度）
✅ 正确示例:
- 主角即将说出凶手是谁 → 被打断 → 观众迫切想知道
- 女主即将答应求婚 → 被打断 → 观众迫切想知道结局
- 秘密即将揭晓 → 被打断 → 观众迫切想知道是什么

❌ 错误示例:
- 打断后观众不关心后续
- 打断后情节没有变化
- 打断后继续正常的对话

**评分标准**:
- 满足3个条件: 置信度8-10分（可以标记）
- 只满足2个条件: 置信度6-7分（谨慎标记）
- 只满足1个条件: 置信度<6分（不要标记）

---

## 🎯 钩子强度评分标准（0-10分）

### 9-10分：超强钩子（必须标记）
**特征**:
- 剧情发生重大转折（重生、穿越、身份揭晓）
- 关键秘密即将揭晓但被打断
- 主角生命/命运受到威胁的悬念
- 观众会疯狂想知道后续的时刻

**示例**:
- 重生剧：主角重生后复仇决心刚刚表达，画面停在这里
- 悬疑剧：凶手即将被说出名字时被打断
- 偶像剧：女主即将答应求婚时被打断

### 7-8分：强钩子（应该标记）
**特征**:
- 明显的情节转折（反转、突变）
- 新信息突然出现（秘密揭露、真相揭示）
- 冲突即将爆发（争吵前一刻、对抗开始前）
- 情感达到高点（愤怒、震惊、崩溃）

**示例**:
- 情感爆发后画面停在悬念处
- 突然出现新角色/新信息
- 决定性时刻的前一秒

### 6-7分：中等钩子（谨慎标记）
**特征**:
- 有一定转折，但不够强烈
- 信息有一定重要性，但不是关键
- 有悬念，但不是"迫切想知道"

**判断**: 如果是6.5分以下，不要标记

### <6分：弱钩子/不是钩子（不要标记）
**特征**:
- 普通对话结束
- 场景切换
- 情节正常推进
- 不重要的信息被截断

**判断**: 宁可不标记，也不要标记弱钩子

---

## 📺 分析片段信息

时间范围：{START}-{END}秒（第{EPISODE}集，窗口时长{DURATION}秒）
关键帧：[按时间顺序排列，每帧都有时间戳]
ASR文本：{ASR_TEXT}

---

## 🎨 可选的高光类型（必须匹配特征才能标记）
{HIGHLIGHT_TYPES}

---

## 🎨 可选的钩子类型（必须匹配特征才能标记）
{HOOK_TYPES}

---

## 🔍 分析步骤

1. **仔细观察**关键帧的时间变化和画面内容
2. **理解情节**：阅读ASR文本，理解剧情走向
3. **判断类型**：检查窗口内容是否匹配上述某个类型的特征
4. **验证强度**：
   - 如果是"对话突然中断" → 必须验证是否满足3个严格条件
   - 如果是其他类型（情节转折、秘密揭露等）→ 有明显钩子特征即可
5. **评分**：根据钩子强度评分标准，给出0-10分
6. **决策**：
   - 情节转折、秘密揭露、情感爆发等 → ≥6.5分即可标记
   - "对话突然中断" → 必须≥7分且满足3个条件才标记
   - 其他类型 → ≥6.5分且有明显钩子特征即可标记

---

## ⚙️ 特别规则

1. **第1集开头**: 如果是第1集的0-30秒范围，自动识别为"开篇高光点"
2. **类型匹配**: 优先匹配上述类型，实在匹配不上可以标记为"未知"
3. **置信度评分**:
   - 情节转折、秘密揭露、情感爆发等: 6.5分以上即可标记
   - "对话突然中断": 必须7分以上且满足3个条件
4. **平衡原则**: 不要漏掉明显的钩子点，但"对话突然中断"要严格筛选

---

## 📤 输出格式

```json
{{
  "highlight": {{
    "exists": true/false,
    "preciseSecond": 125,
    "type": "从上述类型中选择，如：开篇高光",
    "confidence": 8.5,
    "reasoning": "女主重生后表达复仇决心，情感强烈，引发观众期待"
  }},
  "hook": {{
    "exists": true/false,
    "preciseSecond": 366,
    "type": "从上述类型中选择，如：悬念反转",
    "confidence": 9.2,
    "reasoning": "男主突然说出关键秘密，画面停在悬念时刻，观众迫切想知道后续"
  }}
}}
```

**只返回JSON，不要其他文字。**
"""


@dataclass
class SegmentAnalysis:
    """片段分析结果"""
    episode: int
    start_time: int
    end_time: int

    # 高光点数据
    is_highlight: bool
    highlight_timestamp: int  # 精确时间戳
    highlight_type: Optional[str]
    highlight_desc: str
    highlight_confidence: float  # 置信度 0-10

    # 钩子点数据
    is_hook: bool
    hook_timestamp: int  # 精确时间戳
    hook_type: Optional[str]
    hook_desc: str
    hook_confidence: float  # 置信度 0-10


def encode_image(image_path: str) -> str:
    """将图片编码为base64"""
    with open(image_path, 'rb') as f:
        return base64.b64encode(f.read()).decode('utf-8')


def build_analyze_prompt(segment: VideoSegment, skill_framework: dict) -> dict:
    """构建分析请求

    Args:
        segment: 视频片段
        skill_framework: 技能框架

    Returns:
        API请求payload
    """
    # 合并ASR文本
    asr_text = ' '.join(seg.text for seg in segment.asr_segments)
    if not asr_text:
        asr_text = "(无语音)"

    # 计算窗口时长
    duration = segment.end_time - segment.start_time

    # 提取类型信息用于Prompt
    highlight_types_text = format_types_for_prompt(skill_framework.get('highlight_types', []))
    hook_types_text = format_types_for_prompt(skill_framework.get('hook_types', []))

    # 构建prompt
    prompt = ANALYZE_PROMPT.format(
        START=segment.start_time,
        END=segment.end_time,
        EPISODE=segment.episode,
        DURATION=duration,
        ASR_TEXT=asr_text[:800],
        HIGHLIGHT_TYPES=highlight_types_text,
        HOOK_TYPES=hook_types_text
    )

    # 准备关键帧图片
    keyframe_parts = []
    for kf in segment.keyframes[:5]:  # 最多5张
        if segment.keyframes.index(kf) >= 5:
            break
        if segment.keyframes.index(kf) % 3 == 0:  # 每3张取1张，减少API调用
            try:
                img_base64 = encode_image(kf.frame_path)
                keyframe_parts.append({
                    "inline_data": {
                        "mime_type": "image/jpeg",
                        "data": img_base64
                    }
                })
            except Exception as e:
                print(f"警告: 无法编码图片 {kf.frame_path}: {e}")

    # 构建请求
    parts = [{"text": prompt}]
    parts.extend(keyframe_parts)

    return {
        "contents": [{"parts": parts}],
        "generationConfig": {
            "temperature": TrainingConfig.GEMINI_TEMPERATURE,
            "maxOutputTokens": 1024
        }
    }


def format_types_for_prompt(types: list) -> str:
    """将类型列表格式化为Prompt文本

    Args:
        types: 类型列表（从技能JSON中提取）

    Returns:
        格式化的文本
    """
    if not types:
        return "（无）"

    lines = []
    for t in types[:10]:  # 最多显示10个类型
        name = t.get('name', '未知')
        desc = t.get('description', '')
        required = t.get('required_features', {})

        # 提取关键特征
        keywords = required.get('dialogue_keywords', [])[:3]
        emotion = required.get('emotion_keywords', [])[:2]

        lines.append(f"- **{name}**: {desc}")

        if keywords:
            lines.append(f"  - 关键词: {', '.join(keywords)}")
        if emotion:
            lines.append(f"  - 情绪: {', '.join(emotion)}")

    return '\n'.join(lines)


def parse_analysis_response(response_text: str, segment_start: int, segment_end: int) -> dict:
    """解析V6响应 - 包含精确时间戳和置信度

    Args:
        response_text: API响应文本
        segment_start: 窗口开始时间
        segment_end: 窗口结束时间

    Returns:
        分析结果dict
    """
    # 清理markdown
    clean = response_text.strip()
    if clean.startswith('```json'):
        clean = clean[7:]
    if clean.startswith('```'):
        clean = clean[3:]
    if clean.endswith('```'):
        clean = clean[:-3]

    # 提取JSON
    json_match = re.search(r'\{[\s\S]*\}', clean)
    if not json_match:
        return _get_empty_result(segment_start)

    try:
        data = json.loads(json_match.group())
    except json.JSONDecodeError:
        return _get_empty_result(segment_start)

    # 验证并提取高光点数据
    highlight_data = data.get("highlight", {})
    if highlight_data.get("exists", False):
        hl_timestamp = highlight_data.get("preciseSecond")
        # 验证时间戳在窗口内
        if hl_timestamp is None or not (segment_start <= hl_timestamp <= segment_end):
            hl_timestamp = segment_start  # 超出范围使用默认值
    else:
        hl_timestamp = None

    # 验证并提取钩子点数据
    hook_data = data.get("hook", {})
    if hook_data.get("exists", False):
        hook_timestamp = hook_data.get("preciseSecond")
        # 验证时间戳在窗口内
        if hook_timestamp is None or not (segment_start <= hook_timestamp <= segment_end):
            hook_timestamp = segment_start  # 超出范围使用默认值
    else:
        hook_timestamp = None

    # 提取悬念分析字段（V10新增）
    suspense_analysis = hook_data.get("suspense_analysis", {})

    return {
        "highlight": {
            "exists": highlight_data.get("exists", False),
            "preciseSecond": hl_timestamp,
            "type": highlight_data.get("type"),
            "confidence": highlight_data.get("confidence", 0.0),
            "reasoning": highlight_data.get("reasoning", "")
        },
        "hook": {
            "exists": hook_data.get("exists", False),
            "preciseSecond": hook_timestamp,
            "type": hook_data.get("type"),
            "confidence": hook_data.get("confidence", 0.0),
            "reasoning": hook_data.get("reasoning", ""),
            "suspense_analysis": suspense_analysis
        }
    }


def _get_empty_result(segment_start: int) -> dict:
    """返回空结果"""
    return {
        "highlight": {
            "exists": False,
            "preciseSecond": None,
            "type": None,
            "confidence": 0.0,
            "reasoning": ""
        },
        "hook": {
            "exists": False,
            "preciseSecond": None,
            "type": None,
            "confidence": 0.0,
            "reasoning": ""
        }
    }


def analyze_segment(
    segment: VideoSegment,
    skill_framework: dict,
    max_retries: int = 3
) -> SegmentAnalysis:
    """分析单个片段
    
    Args:
        segment: 视频片段
        skill_framework: 技能框架
        max_retries: 最大重试次数
        
    Returns:
        分析结果
    """
    url = f"https://yunwu.ai/v1beta/models/gemini-2.0-flash:generateContent?key={TrainingConfig.GEMINI_API_KEY}"
    headers = {"Content-Type": "application/json"}
    
    for attempt in range(max_retries):
        try:
            payload = build_analyze_prompt(segment, skill_framework)
            
            response = requests.post(
                url, headers=headers, json=payload, timeout=60
            )
            response.raise_for_status()
            
            data = response.json()
            response_text = data['candidates'][0]['content']['parts'][0]['text']
            
            # 使用新的解析函数（传入窗口边界）
            result = parse_analysis_response(
                response_text,
                segment.start_time,
                segment.end_time
            )

            # 提取数据
            hl_data = result.get("highlight", {})
            hook_data = result.get("hook", {})

            return SegmentAnalysis(
                episode=segment.episode,
                start_time=segment.start_time,
                end_time=segment.end_time,

                # 高光点
                is_highlight=hl_data.get("exists", False),
                highlight_timestamp=hl_data.get("preciseSecond", segment.start_time),
                highlight_type=hl_data.get("type"),
                highlight_desc=hl_data.get("reasoning", ""),
                highlight_confidence=hl_data.get("confidence", 0.0),

                # 钩子点
                is_hook=hook_data.get("exists", False),
                hook_timestamp=hook_data.get("preciseSecond", segment.start_time),
                hook_type=hook_data.get("type"),
                hook_desc=hook_data.get("reasoning", ""),
                hook_confidence=hook_data.get("confidence", 0.0)
            )
            
        except Exception as e:
            if attempt < max_retries - 1:
                wait_time = (2 ** attempt) * 5
                print(f"分析片段 {segment.start_time}-{segment.end_time}秒失败，{wait_time}秒后重试...")
                time.sleep(wait_time)
            else:
                print(f"分析片段失败: {e}")
                return SegmentAnalysis(
                    episode=segment.episode,
                    start_time=segment.start_time,
                    end_time=segment.end_time,
                    is_highlight=False,
                    highlight_type=None,
                    highlight_desc="分析失败",
                    is_hook=False,
                    hook_type=None,
                    hook_desc="分析失败"
                )


def analyze_all_segments(
    segments: List[VideoSegment],
    skill_framework: dict,
    max_concurrent: int = 3
) -> List[SegmentAnalysis]:
    """并行分析所有片段
    
    Args:
        segments: 片段列表
        skill_framework: 技能框架
        max_concurrent: 最大并发数
        
    Returns:
        所有分析结果
    """
    results = []
    total = len(segments)
    
    with ThreadPoolExecutor(max_workers=max_concurrent) as executor:
        futures = {
            executor.submit(analyze_segment, seg, skill_framework): seg 
            for seg in segments
        }
        
        for i, future in enumerate(as_completed(futures), 1):
            result = future.result()
            results.append(result)
            
            # 打印进度
            if result.is_highlight or result.is_hook:
                marker = []
                if result.is_highlight:
                    marker.append(f"高光:{result.highlight_type}")
                if result.is_hook:
                    marker.append(f"钩子:{result.hook_type}")
                print(f"[{i}/{total}] {result.start_time}-{result.end_time}秒: {', '.join(marker)}")
            else:
                print(f"[{i}/{total}] {result.start_time}-{result.end_time}秒: 无")
    
    return results


if __name__ == "__main__":
    print("analyze_segment 模块已加载")
