# 杭州雷鸣 AI 短剧剪辑服务 - Python 依赖
# 安装：pip install -r requirements.txt
# 完整安装（含FFmpeg）：bash install.sh

# ── 视频处理 ─────────────────────────────────────────────────────────────────
ffmpeg-python>=0.2.0        # FFmpeg Python 绑定（FFmpeg 本体需系统单独安装）

# ── 数值计算 ──────────────────────────────────────────────────────────────────
numpy>=1.24.0               # 数值计算基础库（OpenCV/PaddleOCR/Whisper 共同依赖）

# ── ASR（语音识别）────────────────────────────────────────────────────────────
openai-whisper>=20231117    # OpenAI Whisper 语音转文字
torch>=2.0.0                # PyTorch（Whisper 依赖）
torchaudio>=2.0.0           # Torch Audio（Whisper 依赖）

# ── OCR（敏感词检测，V16+）────────────────────────────────────────────────────
paddlepaddle>=2.6.0         # PaddlePaddle 深度学习框架（PaddleOCR 依赖）
paddleocr>=2.7.0            # PaddleOCR 3.x 字幕识别

# ── 视觉分析 ──────────────────────────────────────────────────────────────────
opencv-python>=4.8.0        # OpenCV 画面相似度检测（V14.8+）
imagehash>=4.3.0            # 图像哈希（画面相似度检测）
Pillow>=10.0.0              # 图像处理（角标渲染 badge_renderer，V18+）

# ── AI 分析 ───────────────────────────────────────────────────────────────────
google-generativeai>=0.3.0  # Gemini API（视频理解 + 片段分析）
pydantic>=2.0.0             # 数据验证

# ── 数据处理 ──────────────────────────────────────────────────────────────────
pandas>=2.0.0               # Excel 训练数据读取
openpyxl>=3.1.0             # Excel 文件读写

# ── 工具库 ────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # 环境变量管理（GEMINI_API_KEY 等）
tqdm>=4.65.0                # 进度条显示
requests>=2.31.0            # HTTP 请求
